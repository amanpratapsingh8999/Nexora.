NexoraWebsite/          ← root folder
   ├── index.html       ← main website code (ready below)
   ├── style.css        ← optional (agar alag CSS use karna ho)
   └── images/          ← optional (agar aap local images rakhna chaho)
